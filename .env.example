# Dev local uniquement — Vite injecte cette variable via import.meta.env
VITE_AIS_API_KEY=your_aisstream_api_key_here

# Production Vercel — NE PAS définir ici, configurer dans Vercel Dashboard
# Settings → Environment Variables → AIS_API_KEY (scope: Production)
# Servi au client via la serverless function /api/ais-config
# AIS_API_KEY=
